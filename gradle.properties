org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.configuration-cache=true
kotlin.code.style=official
kotlin.incremental=true
kotlin.native.cacheKind.linuxX64=none
android.useAndroidX=true
android.nonTransitiveRClass=true
# Temporary AGP 9 compatibility switch: Kotlin 2.4.0 still decorates KotlinAndroidTarget
# against the legacy variant API in this mixed Android/KMP build. Keep built-in Kotlin
# enabled; remove this opt-out when the Kotlin/AGP combination no longer requires it.
android.newDsl=false
